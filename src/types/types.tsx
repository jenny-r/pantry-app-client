export enum PantryMode {
    Default,
    Edit,
    Delete,
    Add,
    AddToGrocery
}

export enum PantrySort {
    Name = 'Name',
    Quantity = 'Quantity'
}

export enum GroceryMode {
    Default,
    Edit,
    Delete,
    Add
}

export enum GrocerySort {
    Name = 'Name',
    Quantity = 'Quantity'
}